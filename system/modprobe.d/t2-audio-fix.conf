# snd_soc_avs drives 00:1f.3 (Intel SST/AVS) but T2 handles all audio.
# Blacklisting it prevents its entire dep chain (snd_soc_core, snd_hda_codec, etc.)
# from racing with t2bce_audio for PCM registration at boot.
blacklist snd_soc_avs
