# Legacy Hyprland Config Archive

Snapshot of pre-Lua (Hyprland < 0.55) `.conf` files plus migration `.bak`/`.new`/`.old`/`.backup` artifacts, captured on 2026-06-14 before being removed from the live config tree.

Hyprland 0.55+ here uses `hyprland.lua` -> `hyprland/*.lua` (defaults) + `custom/*.lua` (user overrides) — see `CLAUDE.md`. Nothing below is read by the running config; kept for reference/recovery only.

## Contents

- `hyprland.conf`
- `hyprland.conf.new`
- `hyprland.conf.old`
- `binds.conf`
- `colors.conf`
- `variables.conf`
- `monitors.conf`
- `monitors.conf.new`
- `workspaces.conf`
- `workspaces.conf.new`
- `hypridle.conf.new`
- `hypridle.conf.old`
- `hyprlock.conf.new`
- `hyprlock.conf.old`
- `environment.backup`
- `custom/env.conf`
- `custom/env.conf.bak`
- `custom/execs.conf`
- `custom/execs.conf.bak`
- `custom/general.conf`
- `custom/general.conf.bak`
- `custom/keybinds.conf`
- `custom/keybinds.conf.bak`
- `custom/rules.conf`
- `custom/rules.conf.bak`

## `hyprland.conf`

```ini


# This config is a STUB! This should never be generated.
# Use the default lua config from https://github.com/hyprwm/Hyprland/blob/main/example/hyprland.lua

autogenerated = 1

$mainMod = SUPER
$terminal = kitty
$fileManager = dolphin
$menu = hyprlauncher

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu

```

## `hyprland.conf.new`

```ini
# This file sources other files in `hyprland` and `custom` folders
# You wanna add your stuff in files in `custom`

$qsConfig = ii
exec = hyprctl dispatch submap global # DO NOT REMOVE THIS OR YOU WON'T BE ABLE TO USE ANY KEYBIND
submap = global # This is required for catchall to work

# Environment variables
source=hyprland/env.conf
source=custom/env.conf

# Defaults
source=hyprland/execs.conf
source=hyprland/general.conf
source=hyprland/rules.conf
source=hyprland/colors.conf
source=hyprland/keybinds.conf

# Custom 
source=custom/execs.conf
source=custom/general.conf
source=custom/rules.conf
source=custom/keybinds.conf

# nwg-displays support
source=workspaces.conf
source=monitors.conf

# Shell overrides
source=hyprland/shellOverrides/main.conf

```

## `hyprland.conf.old`

```ini
# This file sources other files in `hyprland` and `custom` folders
# You wanna add your stuff in files in `custom`

$qsConfig = ii
exec = hyprctl dispatch submap global # DO NOT REMOVE THIS OR YOU WON'T BE ABLE TO USE ANY KEYBIND
submap = global # This is required for catchall to work

# Defaults
source=hyprland/env.conf
source=hyprland/execs.conf
source=hyprland/general.conf
source=hyprland/rules.conf
source=hyprland/colors.conf
source=hyprland/keybinds.conf

# Custom 
source=custom/env.conf
source=custom/execs.conf
source=custom/general.conf
source=custom/rules.conf
source=custom/keybinds.conf

# nwg-displays support
source=workspaces.conf
source=monitors.conf

```

## `binds.conf`

```ini
# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Print, exec, grim -t png -l 0
bind = $mainMod, W, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, P, pseudo,
bind = $mainMod, G, togglegroup,
bind = $mainMod, X, togglesplit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
#bind = $mainMod, D, hyprexpo:expo, toggle
#bind = $mainMod, space, exec, pkill $menu || $menu --show drun,
bind = $mainMod, space, exec, zsh -c "pkill rofi || rofi -show drun"
bind = $mainMod, F, fullscreen
bind = $mainMod, bracketright, exec, /home/peef/scripts/change-wallpaper.sh
bind = $mainMod, B, exec, $browser
bind = $mainMod SHIFT, C, exec, /home/peef/scripts/hyprshot-region.sh

bindl = $mainMod SHIFT, B, exec, /home/peef/scripts/zathura-launcher.sh
bindl = $mainMod SHIFT, D, exec, /home/peef/scripts/rofi-dict.sh
bindl = $mainMod SHIFT, T, exec, hyprlock
# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bindl = ,mouse:276, workspace, e+1
bindl = ,mouse:275, workspace, e-1

#Switch tabs in group with mainMod + LALT_H and LALT_L
bind = $mainMod SHIFT, H, changegroupactive, b
bind = $mainMod SHIFT, L, changegroupactive, f

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = ALT, LEFT, resizeactive, -50 0
bind = ALT, RIGHT, resizeactive, 50 0
bind = ALT, UP, resizeactive, 0 -50
bind = ALT, DOWN, resizeactive, 0 50

# Volume
# Mute
bind = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle

# Volume Down
bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%

# Volume Up
bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%



# Brightness
bind = ,XF86MonBrightnessUp, exec, brightnessctl set +5%
bind = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-
bind = ALT, XF86MonBrightnessUp, exec, brightnessctl set +1%
bind = ALT, XF86MonBrightnessDown, exec, brightnessctl set 1%-
# Play, Pause, Next

# Play/Pause
bind = ,XF86AudioPlay, exec, playerctl play-pause

# Next Track
bind = ,XF86AudioNext, exec, playerctl next

# Previous Track
bind = ,XF86AudioPrev, exec, playerctl previous

#Backlit
bind = ,XF86KbdBrightnessUp, exec, brightnessctl -sd :white:kbd_backlight set 100
bind = ,XF86KbdBrightnessDown, exec,  brightnessctl -sd :white:kbd_backlight set 0

bind = ALT, XF86KbdBrightnessUp, exec, brightnessctl -sd appletb_backlight set +1

bind = ALT, XF86KbdBrightnessDown, exec, brightnessctl -sd appletb_backlight set -1



```

## `colors.conf`

```ini

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$background = rgba(1a1110ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$error = rgba(ffb4abff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$error_container = rgba(93000aff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$inverse_on_surface = rgba(392e2cff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$inverse_primary = rgba(904b3fff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$inverse_surface = rgba(f1dfdbff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_background = rgba(f1dfdbff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_error = rgba(690005ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_error_container = rgba(ffdad6ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_primary = rgba(561e16ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_primary_container = rgba(ffdad4ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_primary_fixed = rgba(3a0905ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_primary_fixed_variant = rgba(73342aff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_secondary = rgba(442925ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_secondary_container = rgba(ffdad4ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_secondary_fixed = rgba(2c1511ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_secondary_fixed_variant = rgba(5d3f3aff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_surface = rgba(f1dfdbff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_surface_variant = rgba(d8c2beff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_tertiary = rgba(3e2e04ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_tertiary_container = rgba(fbe0a6ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_tertiary_fixed = rgba(251a00ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$on_tertiary_fixed_variant = rgba(564419ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$outline = rgba(a08c89ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$outline_variant = rgba(534341ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$primary = rgba(ffb4a7ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$primary_container = rgba(73342aff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$primary_fixed = rgba(ffdad4ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$primary_fixed_dim = rgba(ffb4a7ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$scrim = rgba(000000ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$secondary = rgba(e7bdb6ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$secondary_container = rgba(5d3f3aff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$secondary_fixed = rgba(ffdad4ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$secondary_fixed_dim = rgba(e7bdb6ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$shadow = rgba(000000ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$source_color = rgba(ef4d38ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface = rgba(1a1110ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_bright = rgba(423735ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_container = rgba(271d1cff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_container_high = rgba(322826ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_container_highest = rgba(3d3230ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_container_low = rgba(231918ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_container_lowest = rgba(140c0bff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_dim = rgba(1a1110ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_tint = rgba(ffb4a7ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$surface_variant = rgba(534341ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$tertiary = rgba(dec48cff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$tertiary_container = rgba(564419ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$tertiary_fixed = rgba(fbe0a6ff)

$image = /home/peef/Wallpapers/wallhaven-12.jpg
$tertiary_fixed_dim = rgba(dec48cff)


```

## `variables.conf`

```ini
$mainMod = SUPER # Sets "Windows" key as main modifier

$terminal = kitty
$fileManager = nautilus
$menu = rofi
$browser = zen-browser

```

## `monitors.conf`

```ini
# This file is to be overwritten by nwg-displays if you choose to use it.
# nwg-displays is a graphical tool for managing monitors.
# - Installation for Arch Linux: sudo pacman -S nwg-displays
# - Repo: https://github.com/nwg-piotr/nwg-displays

monitor = eDP-1, 2560x1600@60, auto, 1.4

```

## `monitors.conf.new`

```ini
# This file is to be overwritten by nwg-displays if you choose to use it.
# nwg-displays is a graphical tool for managing monitors. 
# - Installation for Arch Linux: sudo pacman -S nwg-displays
# - Repo: https://github.com/nwg-piotr/nwg-displays

```

## `workspaces.conf`

```ini
# This file is to be overwritten by nwg-displays if you choose to use it.
# nwg-displays is a graphical tool for managing monitors. 
# - Installation for Arch Linux: sudo pacman -S nwg-displays
# - Repo: https://github.com/nwg-piotr/nwg-displays

```

## `workspaces.conf.new`

```ini
# This file is to be overwritten by nwg-displays if you choose to use it.
# nwg-displays is a graphical tool for managing monitors. 
# - Installation for Arch Linux: sudo pacman -S nwg-displays
# - Repo: https://github.com/nwg-piotr/nwg-displays

```

## `hypridle.conf.new`

```ini
$lock_cmd = hyprctl dispatch 'hl.dsp.global("quickshell:lock")' & pidof qs quickshell hyprlock || hyprlock
# $lock_cmd = pidof hyprlock || hyprlock
$suspend_cmd = systemctl suspend || loginctl suspend

general {
    lock_cmd = $lock_cmd
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch global quickshell:lockFocus
    inhibit_sleep = 3
}

listener {
    timeout = 300 # 5mins
    on-timeout = loginctl lock-session
}

listener {
    timeout = 600 # 10mins
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 900 # 15mins
    on-timeout = $suspend_cmd
}

```

## `hypridle.conf.old`

```ini
lock = env XDG_SESSION_TYPE=wayland WAYLAND_DISPLAY=wayland-1 hyprlock

general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on  # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 300                                # 2.5min.
    on-timeout = brightnessctl -s set 10         # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r                 # monitor backlight restore.
}

# turn off keyboard backlight, comment out this section if you dont have a keyboard backlight.
listener { 
    timeout = 240                                          # 2.5min.
    on-timeout = brightnessctl -sd :white:kbd_backlight set 0 # turn off keyboard backlight.
    on-resume = brightnessctl -rd :white:kbd_backlight  set 3     # turn on keyboard backlight.
}

listener {
    timeout = 300                                 # 5min
    on-timeout = loginctl lock-session            # lock screen when timeout has passed
}

listener {
    timeout = 330                                                     # 5.5min
    on-timeout = hyprctl dispatch dpms off                            # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r          # screen on when activity is detected after timeout has fired.
}

listener {
    timeout = 1800                                # 30min
    on-timeout = systemctl suspend                # suspend pc
}

```

## `hyprlock.conf.new`

```ini
source=~/.config/hypr/hyprlock/colors.conf

background {
    color = rgba(181818FF)
}
input-field {
    monitor =
    size = 250, 50
    outline_thickness = 2
    dots_size = 0.1
    dots_spacing = 0.3
    outer_color = $entry_border_color
    inner_color = $entry_background_color
    font_color = $entry_color
    fade_on_empty = true

    position = 0, 20
    halign = center
    valign = center
}

label {
    monitor =
    text = $LAYOUT
    color = $text_color
    font_size = 14
    font_family = $font_family
    position = -30, 30
    halign = right
    valign = bottom
}

label { # Caps Lock Warning
    monitor =
    text = cmd[update:250] ${XDG_CONFIG_HOME:-$HOME/.config}/hypr/hyprlock/check-capslock.sh
    color = $text_color
    font_size = 13
    font_family = $font_family
    position = 0, -25
    halign = center
    valign = center
}


label { # Clock
    monitor =
    text = $TIME
    color = $text_color
    font_size = 65
    font_family = $font_family_clock

    position = 0, 300
    halign = center
    valign = center
}
label { # Date
    monitor =
    text = cmd[update:5000] date +"%A, %B %d"
    color = $text_color
    font_size = 17
    font_family = $font_family_clock

    position = 0, 240
    halign = center
    valign = center
}

label { # User
    monitor =
    text =     $USER
    color = $text_color
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    font_size = 20
    font_family = $font_family
    position = 0, 50
    halign = center
    valign = bottom
}

label { # Status
    monitor =
    text = cmd[update:5000] ${XDG_CONFIG_HOME:-$HOME/.config}/hypr/hyprlock/status.sh
    color = $text_color
    font_size = 14
    font_family = $font_family

    position = 30, -30
    halign = left
    valign = top
}
```

## `hyprlock.conf.old`

```ini
source = colors.conf

# BACKGROUND
background {
    monitor =
    path = /home/peef/Wallpapers/wallhaven-12.jpg
    # Blur settings
    blur_passes = 3
    blur_size = 8
    # Adjust these for better visibility with blur
    brightness = 0.8
    contrast = 0.9
}
# INPUT FIELD
input-field {
    monitor = eDP-1
    size = 300, 40
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = $primary_fixed_dim
    inner_color = $primary
    font_color = $surface_container_low
    fade_on_empty = true
    placeholder_text = Enter password...
    hide_input = false
    position = 0, 150
    halign = center
    valign = bottom
}

# Hour-Time
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%H")"
    color = $primary
    font_family = JetBrainsMono Bold
    font_size = 180
    position = 0, 150
    halign = center
    valign = center
}

# Minute-Time
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%M")"
    color = $on_surface
    font_family = JetBrainsMono Bold
    font_size = 180
    position = 0, -75
    halign = center
    valign = center
}

# Date
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%a, %b %d")"
    color = $on_surface
    font_family = JetBrainsMono Bold
    position = 100, -100
    halign = left
    valign = top
}

```

## `environment.backup`

```ini
# Wayland DPI and scale settings
export WLR_DPI=154

# GTK scaling for HiDPI
export GDK_SCALE=1
export GDK_DPI_SCALE=1.6
export GDK_BACKEND=wayland

# QT scaling (for KDE/Qt apps)
export QT_QPA_PLATFORM=wayland
export QT_AUTO_SCREEN_SCALE_FACTOR=0
export QT_SCALE_FACTOR=1

# Cursor size
export XCURSOR_SIZE=32

# XWayland compatibility fallback
export XDG_SESSION_TYPE=wayland
export XDG_CURRENT_DESKTOP=Hyprland
export MOZ_ENABLE_WAYLAND=1          # Firefox
export CLUTTER_BACKEND=wayland       # GNOME apps

# Electron apps (e.g. VS Code, Discord)
export ELECTRON_ENABLE_WAYLAND=1
export SDL_VIDEODRIVER=wayland
export _JAVA_AWT_WM_NONREPARENTING=1


```

## `custom/env.conf`

```ini
# You can put extra environment variables here
# https://wiki.hyprland.org/Configuring/Environment-variables/

# ######### Input method ##########
# See https://fcitx-im.org/wiki/Using_Fcitx_5_on_Wayland
#env = QT_IM_MODULE, fcitx
#env = XMODIFIERS, @im=fcitx
#env = SDL_IM_MODULE, fcitx
#env = GLFW_IM_MODULE, ibus
#env = INPUT_METHOD, fcitx
env = QT_AUTO_SCREEN_SCALE_FACTOR, 1.6
env = QT_ENABLE_HIGHDPI_SCALING, 1


# ######## Browser ##########
env = BROWSER, thorium-browser

# ######## Waylan

```

## `custom/env.conf.bak`

```ini
# You can put extra environment variables here
# https://wiki.hyprland.org/Configuring/Environment-variables/

# ######### Input method ##########
# See https://fcitx-im.org/wiki/Using_Fcitx_5_on_Wayland
#env = QT_IM_MODULE, fcitx
#env = XMODIFIERS, @im=fcitx
#env = SDL_IM_MODULE, fcitx
#env = GLFW_IM_MODULE, ibus
#env = INPUT_METHOD, fcitx
env = QT_AUTO_SCREEN_SCALE_FACTOR, 0
env = QT_SCALE_FACTOR, 1.25
env = QT_FONT_DPI, 90
env = QT_ENABLE_HIGHDPI_SCALING, 1
# ######## Waylan
```

## `custom/execs.conf`

```ini
# You can make apps auto-start here
# Relevant Hyprland wiki section: https://wiki.hyprland.org/Configuring/Keywords/#executing

# Input method
# exec-once = fcitx5


```

## `custom/execs.conf.bak`

```ini
# You can make apps auto-start here
# Relevant Hyprland wiki section: https://wiki.hyprland.org/Configuring/Keywords/#executing

# Input method
# exec-once = fcitx5


```

## `custom/general.conf`

```ini
# Put general config stuff here
# Here's a list of every variable: https://wiki.hyprland.org/Configuring/Variables/

#general {
#layout = scrolling
#}

#gesture = 3, horizontal, layoutmsg, move +col

#bind = $mainMod, period, layoutmsg, move +col
#bind = $mainMod, comma, layoutmsg, move -col
#bind = $mainMod SHIFT, period, layoutmsg, movewindowto r
#bind = $mainMod SHIFT, comma, layoutmsg, movewindowto l
#bind = $mainMod SHIFT, up, layoutmsg, movewindowto u
#bind = $mainMod SHIFT, down, layoutmsg, movewindowto d

#plugin {
#    hyprscrolling {
#        column_width = 0.8
#        fullscreen_on_one_column = false
#    }
#}


# Swap Alt and Super so Cmd = Super (fixes T2 MacBook key mapping)
input {
    kb_options = altwin:swap_alt_win
}

decoration {
    blur {
        xray = false
    }
}

# monitor=,addreserved, 0, 0, 0, 0 # Custom reserved area

# HDMI port: mirror display. To see device name, use `hyprctl monitors`

```

## `custom/general.conf.bak`

```ini
# Put general config stuff here
# Here's a list of every variable: https://wiki.hyprland.org/Configuring/Variables/
general {
layout = scrolling
}

plugin {
    hyprscrolling {
        column_width = 0.7
        fullscreen_on_one_column = false
        follow_focus = true
    }
}

# Disable built-in workspace swipe (using scrolling layout gestures instead)
gestures {
    workspace_swipe_enabled = false
}


# monitor=,addreserved, 0, 0, 0, 0 # Custom reserved area

# HDMI port: mirror display. To see device name, use `hyprctl monitors`

```

## `custom/keybinds.conf`

```ini
# See https://wiki.hyprland.org/Configuring/Binds/
#!
##! User
bind = Ctrl+Super, Slash, exec, xdg-open ~/.config/illogical-impulse/config.json # Edit shell config
bind = Ctrl+Super+Alt, Slash, exec, xdg-open ~/.config/hypr/custom/keybinds.conf # Edit extra keybinds

# Backlight
bind = ,XF86KbdBrightnessUp, exec, brightnessctl -sd :white:kbd_backlight set +25%
bind = ,XF86KbdBrightnessDown, exec,  brightnessctl -sd :white:kbd_backlight set 25%-

# Touchbar
bind = Super, XF86KbdBrightnessUp, exec, brightnessctl -sd appletb_backlight set +25%

bind = Super, XF86KbdBrightnessDown, exec, brightnessctl -sd appletb_backlight set 25%-

# Volume
## Mute
bind = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle

## Volume Down
bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%

## Volume Up
bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%

# Play, Pause, Next

## Play/Pause
bind = ,XF86AudioPlay, exec, playerctl play-pause

## Next Track
bind = ,XF86AudioNext, exec, playerctl next

## Previous Track
bind = ,XF86AudioPrev, exec, playerctl previous

##! Apps
bind = Super, W, exec, thorium-browser # Browser

# Add stuff here
# Use #! to add an extra column on the cheatsheet
# Use ##! to add a section in that column
# Add a comment after a bind to add a description, like above


```

## `custom/keybinds.conf.bak`

```ini
# See https://wiki.hyprland.org/Configuring/Binds/
#!
##! User
bind = Ctrl+Super, Slash, exec, xdg-open ~/.config/illogical-impulse/config.json # Edit shell config
bind = Ctrl+Super+Alt, Slash, exec, xdg-open ~/.config/hypr/custom/keybinds.conf # Edit extra keybinds

# Backlight
bind = ,XF86KbdBrightnessUp, exec, brightnessctl -sd :white:kbd_backlight set +25%
bind = ,XF86KbdBrightnessDown, exec,  brightnessctl -sd :white:kbd_backlight set 25%-

# Touchbar
bind = Super, XF86KbdBrightnessUp, exec, brightnessctl -sd appletb_backlight set +25%

bind = Super, XF86KbdBrightnessDown, exec, brightnessctl -sd appletb_backlight set 25%-

# Volume
## Mute
bind = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle

## Volume Down
bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%

## Volume Up
bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%

# Play, Pause, Next

## Play/Pause
bind = ,XF86AudioPlay, exec, playerctl play-pause

## Next Track
bind = ,XF86AudioNext, exec, playerctl next

## Previous Track
bind = ,XF86AudioPrev, exec, playerctl previous

##! Apps
# bind = Super, Return, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "${TERMINAL}" "kitty -1" "foot" "alacritty" "wezterm" "konsole" "kgx" "uxterm" "xterm" # Terminal
# bind = Super, T, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh  "${TERMINAL}" "kitty -1" "foot" "alacritty" "wezterm" "konsole" "kgx" "uxterm" "xterm" # [hidden] (terminal) (alt)
# bind = Ctrl+Alt, T, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "${TERMINAL}" "kitty -1" "foot" "alacritty" "wezterm" "konsole" "kgx" "uxterm" "xterm" # [hidden] (terminal) (for Ubuntu people)
# bind = Super, E, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "dolphin" "nautilus" "nemo" "thunar" "${TERMINAL}" "kitty -1 fish -c yazi" # File manager
# bind = Super, W, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "google-chrome-stable" "zen-browser" "firefox" "brave" "chromium" "microsoft-edge-stable" "opera" "librewolf" # Browser
# bind = Super, C, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "code" "codium" "cursor" "zed" "zedit" "zeditor" "kate" "gnome-text-editor" "emacs" "command -v nvim && kitty -1 nvim" "command -v micro && kitty -1 micro" # Code editor
# bind = Ctrl+Super+Shift+Alt, W, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "wps" "onlyoffice-desktopeditors" "libreoffice" # Office software
# bind = Super, X, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "kate" "gnome-text-editor" "emacs" # Text editor
# bind = Ctrl+Super, V, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "pavucontrol-qt" "pavucontrol" # Volume mixer
# bind = Super, I, exec, XDG_CURRENT_DESKTOP=gnome ~/.config/hypr/hyprland/scripts/launch_first_available.sh "qs -p ~/.config/quickshell/$qsConfig/settings.qml" "systemsettings" "gnome-control-center" "better-control" # Settings app
# bind = Ctrl+Shift, Escape, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "gnome-system-monitor" "plasma-systemmonitor --page-name Processes" "command -v btop && kitty -1 fish -c btop" # Task manager

# Add stuff here
# Use #! to add an extra column on the cheatsheet
# Use ##! to add a section in that column
# Add a comment after a bind to add a description, like above

##! Scrolling Layout
# Column navigation
bind = Super, Period, layoutmsg, move +col # Scroll right (next column)
bind = Super, Comma, layoutmsg, move -col # Scroll left (prev column)
binde = Super+Ctrl, Period, layoutmsg, move +200 # Scroll right (fine)
binde = Super+Ctrl, Comma, layoutmsg, move -200 # Scroll left (fine)

# Move window between columns
bind = Super+Shift, Period, layoutmsg, movewindowto r # Move window right
bind = Super+Shift, Comma, layoutmsg, movewindowto l # Move window left
bind = Super+Shift+Ctrl, Up, layoutmsg, movewindowto u # Move window up in column
bind = Super+Shift+Ctrl, Down, layoutmsg, movewindowto d # Move window down in column

# Column resize
binde = Super+Alt, Period, layoutmsg, colresize +0.1 # Widen column
binde = Super+Alt, Comma, layoutmsg, colresize -0.1 # Narrow column
bind = Super+Alt, Equal, layoutmsg, colresize all # Reset all columns

# Layout actions
bind = Super+Ctrl, F, layoutmsg, fit active # Fit active column
bind = Super+Ctrl+Shift, F, layoutmsg, fit all # Fit all columns
bind = Super+Ctrl, P, layoutmsg, promote # Promote window to new column
bind = Super+Ctrl, bracketleft, layoutmsg, swapcol l # Swap column left
bind = Super+Ctrl, bracketright, layoutmsg, swapcol r # Swap column right
bind = Super+Ctrl, T, layoutmsg, togglefit # Toggle fit method

# Three-finger swipe for scrolling columns (standard Hyprland syntax)
# Overrides workspace swipe - swipe right moves view right, swipe left moves view left
bindgesture = swipe:3:r, layoutmsg, move +col
bindgesture = swipe:3:l, layoutmsg, move -col


```

## `custom/rules.conf`

```ini
# You can put custom rules here
# Window/layer rules: https://wiki.hyprland.org/Configuring/Window-Rules/
# Workspace rules: https://wiki.hyprland.org/Configuring/Workspace-Rules/

# ######## Window rules ########

# Enable blur + transparency for kitty
windowrule = no_blur off, match:class kitty
windowrule = opacity 0.7 override 0.7 override, match:class kitty

# Uncomment to apply global transparency to all windows:
# windowrule = opacity 0.89 override 0.89 override, match:class .*

# Disable blur for all xwayland apps
# windowrule = no_blur on, match:xwayland 1

# ######## Layer rules ########

# Desktop folder widget blur
layerrule = match:namespace quickshell:desktopfolder, blur on
layerrule = match:namespace quickshell:desktopfolder, xray off
layerrule = match:namespace quickshell:desktopfolder, ignore_alpha 0

```

## `custom/rules.conf.bak`

```ini
# You can put custom rules here
# Window/layer rules: https://wiki.hyprland.org/Configuring/Window-Rules/
# Workspace rules: https://wiki.hyprland.org/Configuring/Workspace-Rules/

# ######## Window rules ########

# Uncomment to apply global transparency to all windows:
# windowrule = opacity 0.89 override 0.89 override, match:class .*

# Disable blur for all xwayland apps
# windowrule = no_blur on, match:xwayland 1

```

